module.exports = function (req, res) {
    let body = req.body;
    let pid = body.pid;
    let uid = body.uid;
    if ((uid!=null)&&(uid!=undefined)){
        //Get products recomendations for curret(fixed) user
    }
};